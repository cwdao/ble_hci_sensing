---
description: 新 plan 必须先审查确认，无待确认项才可开始执行
alwaysApply: true
---

# 新 Plan：先确认，再执行

每次收到新的 / 更新的 `docs/plans/*_plan.md`（或用户要求执行某 plan）时：

## 必须先做

1. **通读 plan**，对照模板与既有代码/结果，检查可执行性。
2. **向用户列出**需要额外确认的事项（含 plan 内 `[待确认]`、路径/命名冲突、实验范围歧义、与既有结论矛盾处）。
3. **等待用户回复**后，才能写代码、跑实验或改 `src/` / `notebooks/scripts/` / `outputs/`。

## 无待确认项时

若审查后**确实没有**需要用户拍板的问题，可直接进入执行，但首条回复仍须简短说明：

```text
Plan 审查通过，无待确认项，开始执行。
```

## 禁止

- 禁止在存在歧义或 `[待确认]` 未关闭时擅自选默认值继续。
- 禁止把「实现细节偏好」当成阻塞项反复追问；只确认会影响实验定义、baseline、指标或结论边界的事项。
